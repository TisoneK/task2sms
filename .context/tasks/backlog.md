# Backlog (append-only)

Open items for future sessions. Append at the bottom; never delete or
reorder. When an item is done, check it off and note the session/commit —
don't remove the line.

---
- [ ] **F1: Replace `eval()` on monitor expressions with AST whitelist** (added 2026-07-13 by Super Z) — `app/services/integrations/scraper_service.py:460,497,520` evaluate user-supplied `multi_field_expression` and `monitor_conditions[*].comparing_value` via `eval(_py_expr(expr), {"__builtins__": {}}, namespace)`. The `__builtins__: {}` sandbox is bypassable via `().__class__.__bases__[0].__subclasses__()` → authenticated-user RCE. Severity: **Critical**. Replace with `ast.parse` + `NodeVisitor` whitelist (BinOp, Compare, Name, Constant, BoolOp only) or adopt `simpleeval` library. Needs full rewrite + tests. See `.context/reviews/2026-07-13-review.md` §6 item 1.
- [ ] **F2: Add SSRF protection to outbound HTTP paths** (added 2026-07-13 by Super Z) — Four user-controlled outbound HTTP paths have no URL-scheme validation, no private-IP filtering, and (for the scraper) follow redirects so a public URL returning `Location: http://169.254.169.254/...` bypasses any future IP filter. Locations: `datasource_service.py:19,39`; `scraper_service.py:39,1005`; `webhook_service.py:57`. Severity: **Critical**. Add `validate_outbound_url()` helper that (1) enforces `scheme in {http,https}`, (2) resolves hostname and rejects private/loopback/link-local IPs, (3) pins resolved IP for the request (no DNS rebinding), (4) sets `follow_redirects=False` or re-validates each redirect. Needs security design review of allowed destinations.
- [ ] **F-C1: Migrate JWT from localStorage to httpOnly cookie + add CSP** (added 2026-07-13 by Super Z) — `src/store/authStore.js:6,13,27` stores the JWT in `localStorage` (XSS-exfiltrable); `nginx.conf` has no CSP header. Severity: **Critical**. Migrate to httpOnly + Secure + SameSite=Strict cookie set by the backend on `/api/auth/login`; add a strict CSP. Needs backend coordination.
- [ ] **F8: Persist APScheduler jobstore in DB; stop re-firing all monitors on restart** (added 2026-07-13 by Super Z) — `app/workers/scheduler.py:17,167` uses `AsyncIOScheduler()` with default `MemoryJobStore`. On restart, `load_all_monitors()` re-creates jobs and hard-sets `next_run_time=now+5s` — every active monitor fires 5s after every restart, flooding recipients. Also no `misfire_grace_time`. Severity: **High**. Use `SQLAlchemyJobStore` (persisted in same DB), set `coalesce=True, max_instances=1, misfire_grace_time=300` on `add_job`, respect `monitor.last_checked_at` for `next_run_time`. Needs new Alembic migration for the jobstore table.
- [ ] **F9: Fix race condition on counter increments** (added 2026-07-13 by Super Z) — `task_service.py:118,128` and `scraper_service.py:759-777,824-866` do read-modify-write with no row lock. SQLite has no `SELECT FOR UPDATE`. The `/tasks/{id}/run` endpoint and the scheduled `_run_task` can collide — losing increments and double-sending SMS. Severity: **High**. Use atomic `UPDATE tasks SET run_count = run_count + 1 WHERE id = :id`. Touches several services, needs test coverage.
- [ ] **F10: Remove `create_tables()` from lifespan in production** (added 2026-07-13 by Super Z) — `main.py:22` calls `Base.metadata.create_all` on startup, then Alembic is supposed to migrate. Two sources of truth for schema; if model columns drift from migration heads, `create_all` silently creates the new column on fresh DBs but Alembic upgrade fails on existing DBs. Severity: **High**. Remove `create_tables()` from lifespan in production; rely on `alembic upgrade head` at deploy time. Keep `create_tables` only for tests. Deployment-process change.
- [ ] **F12: Add JWT refresh / revocation / blacklist** (added 2026-07-13 by Super Z) — `app/core/security.py:23-29` issues a single long-lived access token (24h default). No `jti`, no server-side revocation list. Stolen tokens cannot be invalidated. Severity: **Medium**. Add `jti` claim, a `revoked_tokens` table, and a middleware check. Or adopt short access tokens (15min) + refresh tokens. Needs DB migration + middleware.
- [ ] **F14: Add missing indexes on hot paths** (added 2026-07-13 by Super Z) — Missing indexes on `notifications(user_id, created_at)`, `tasks(user_id)`, `webhook_deliveries(webhook_id)`, `email_notifications(user_id)`, `whatsapp_messages(user_id)`, `telegram_messages(user_id)`, `scraper_check_logs(monitor_id, checked_at)`. Affects `analytics_service.py`, `stats.py`, and every list endpoint that filters by `user_id` and orders by `created_at`. Severity: **Medium**. Add a new Alembic migration creating these indexes. Quick win.
- [ ] **F16: Replace `str.format()` on user-supplied templates with `string.Template` or jinja2 SandboxedEnvironment** (added 2026-07-13 by Super Z) — `scraper_service.py:929,961` and `task_service.py:88` use `monitor.message_template.format(**context)`. A template like `{value.__class__.__init__.__globals__[SECRET_KEY]}` can read Python globals. Severity: **Medium**. Audit all template variables; switch to `string.Template` (no attribute access) or jinja2 `SandboxedEnvironment`.
- [ ] **F-H4: Add Escape-to-close, focus-trap, focus-restoration to modals** (added 2026-07-13 by Super Z) — Modals (Modal, ConfirmModal, ElementPicker, and 4 inline modals in pages) lack Escape-to-close, focus trap, and focus restoration. Severity: **High** (a11y). The `role=dialog` was added in commit `5a63ae9` but the keyboard/focus behavior is still missing. Best done together with F-H5 and F-M4 (consolidate modal boilerplate into a single shared component that handles Escape, focus trap, and focus restoration).
- [ ] **F-H5: Split ScraperPage.jsx (2563 lines)** (added 2026-07-13 by Super Z) — Wizard + card + log drawer + helpers all in one file. Severity: **High** (maintainability). Split into `ScraperPage/index.jsx`, `MonitorCard.jsx`, `MonitorFormWizard.jsx`, `CheckLogDrawer.jsx`, `helpers.js`.
- [ ] **F7: Change `WebhookCreate.url` and other URL fields from `str` to `HttpUrl`** (added 2026-07-13 by Super Z) — Length limits added in commit `23fe670`; the type change is deferred because it changes the serialization shape and needs verification against the model column type. Severity: **Medium**. Verify Pydantic `HttpUrl` serialization against the SQLAlchemy `String(1000)` column, then change `url` fields on `WebhookCreate`, `MonitorCreate`, `DataSourceCreate` to `HttpUrl`.
- [ ] **F13: Remove `get_db()` auto-commit; require explicit `db.commit()` in routes** (added 2026-07-13 by Super Z) — `app/core/database.py:21-30` auto-commits on success, including GETs. Also commits after swallowed HTTPException. Severity: **Medium**. Touches every route's behavior — needs careful migration. Mostly a Postgres perf issue; SQLite unaffected.
- [ ] **F19: Add `try/except IntegrityError` to org slug creation** (added 2026-07-13 by Super Z) — `org_service.py:18-21` — two concurrent `create_org` calls with the same name can both pass the `existing.scalar_one_or_none()` check and both insert; the DB unique constraint rejects one with `IntegrityError` but the service doesn't handle it. Severity: **Medium**. Retry with `{slug}-{creator_id}-{random}`.
- [ ] **F22: Bump remaining stale dependency pins** (added 2026-07-13 by Super Z) — `requirements.txt` has exact pins from mid-2024: `fastapi==0.111.0`, `pydantic==2.9.2`, `sqlalchemy==2.0.30`, `httpx==0.27.0`, `playwright==1.44.0`. All have newer patch releases with security fixes. Severity: **Low**. `jose`/`jinja2`/`passlib` already bumped in `d21fa80`; bump the rest incrementally with test runs.
- [ ] **F-M1: Refactor 401 interceptor to clear auth store + use router navigate (not hard redirect)** (added 2026-07-13 by Super Z) — `src/services/api.js` response interceptor does `window.location.href = '/login'` on 401, losing SPA state. Severity: **Medium**. Clear auth store and use `navigate('/login')` via a router-aware mechanism.
- [ ] **F-M2: Adopt React Hook Form + Zod (deps present but unused)** (added 2026-07-13 by Super Z) — All forms use plain `useState`. Severity: **Medium**. Incremental migration starting with login/register, then task create.
- [ ] **F-M3: Move task search server-side** (added 2026-07-13 by Super Z) — `useTasks` filters client-side on a single page. Severity: **Medium**. Add server-side search endpoint.
- [ ] **F-M4: Consolidate modal boilerplate** (added 2026-07-13 by Super Z) — 5 places reimplement the shared `<Modal>` inline. Severity: **Medium**. Refactor to use the shared component everywhere; pair with F-H4.
- [ ] **F-M5: Add `<label htmlFor>` association to form inputs** (added 2026-07-13 by Super Z) — Form inputs lack associated labels. Severity: **Medium** (a11y).
- [ ] **F-M6: Add top-level ErrorBoundary + working 'Try again'** (added 2026-07-13 by Super Z) — 'Try again' button doesn't actually retry. Severity: **Medium**.
- [ ] **F-M8: Make brand colors theme-aware** (added 2026-07-13 by Super Z) — Hardcoded `#fee2e2`, `#fef9c3`, `#dc2626`, `#d97706` in `ConfirmModal` break in dark mode. Severity: **Medium**. Theme refactor.
- [ ] **F21: Collapse duplicated service tree** (added 2026-07-13 by Super Z) — `app/services/{datasource_service,scraper_service,...}.py` are 2-line proxy files re-exporting from `core/` or `integrations/`. Severity: **Low**. Pick one location.
- [ ] **F24: Add tests for SSRF, eval-sandbox escape, webhook signature verification, multi-tenant isolation, scheduler restart, retry worker** (added 2026-07-13 by Super Z) — Severity: **Low**. Backlog.
- [ ] **F26: Add SQL log redaction filter** (added 2026-07-13 by Super Z) — `echo=settings.DEBUG` (database.py:8) logs SQL including `hashed_password` and `auth_value` columns unredacted. Severity: **Low**.
- [ ] **F-N1: Add PropTypes or migrate frontend to TypeScript** (added 2026-07-13 by Super Z) — No type safety on the frontend. Severity: **Nice to Have**.
- [ ] **F-L1: Move WS token out of URL query string** (added 2026-07-13 by Super Z) — ScraperPage element picker passes the token as a query param (logged in server access logs). Severity: **Low**. Needs WebSocket auth redesign.
- [ ] **F-L2: Pause ScraperPage 15s poll when empty / in background** (added 2026-07-13 by Super Z) — Severity: **Low**.
- [ ] **F-L3: Fix `e.target` vs `e.currentTarget` in TasksPage:113** (added 2026-07-13 by Super Z) — Same bug pattern as WhatsAppPage (which was fixed in `5a63ae9`). Severity: **Low**.
- [ ] **F-L4: Fix exhaustive-deps violations in `useData`** (added 2026-07-13 by Super Z) — Severity: **Low**.
- [ ] **F-L5: Guard `JSON.parse(localStorage)` at module load in authStore** (added 2026-07-13 by Super Z) — Severity: **Low**. Wrap in try/catch.
- [ ] **Create `CHANGELOG.md` with `[Unreleased]` section** (added 2026-07-13 by Super Z) — No changelog exists; the protocol's Step 14 has nothing to update. Severity: **Nice to Have**. Recommend Keep a Changelog format.
- [ ] **Create `docs/ARCHITECTURE.md`** (added 2026-07-13 by Super Z) — The README is product-focused; an architecture doc would help future agents orient faster. Severity: **Nice to Have**.
- [ ] **Configure lint + typecheck** (added 2026-07-13 by Super Z) — No `ruff` (backend), no `eslint`/`tsc` (frontend). Severity: **Nice to Have**. Would catch many findings automatically.

---
- [x] **F-R1: Fix pre-existing async-driver false-positive warning in `database.py`** (added 2026-07-13 by Super Z, Session 2; **DONE in commit `c107419` same session**) — `app/core/database.py:33` checked `_db_url.drivername.startswith(("aiosqlite", "asyncpg", "asyncmy"))` but the SQLAlchemy drivername for async SQLite is `sqlite+aiosqlite` (not `aiosqlite` alone). The check was always False for SQLite, producing a spurious "DATABASE_URL driver 'sqlite+aiosqlite' is not async... may deadlock" warning on every startup — even though aiosqlite IS async and the app works fine. Fix: `_db_url.drivername.rsplit("+", 1)[-1]` checks the driver part after the `+`. Originally backlogged "for a future session" (a Pitfall #30 violation); fixed on the spot in the self-audit after the user called out uncaught mistakes.
- [ ] **F-R2: Add Brotli/gzip middleware to the single-service deploy** (added 2026-07-13 by Super Z, Session 2) — The old `frontend/nginx.conf` had `gzip on`. The new single-service FastAPI middleware stack (in `main.py`) doesn't compress responses. For low-traffic use this is fine, but if Railway bills by egress or mobile users are on slow links, add `starlette-features`' `GZipMiddleware` (or `BrotliMiddleware` from `brotli-asgi`). Severity: **Nice to Have**. Pair with F10 cleanup (lifespan refactor) — both touch `main.py`'s middleware/startup area.
- [ ] **F-R3: Verify `playwright install-deps chromium` works in the single-service root Dockerfile** (added 2026-07-13 by Super Z, Session 2) — The root `Dockerfile` runs `apt-get install ... && rm -rf /var/lib/apt/lists/*` (line ~52) then later `RUN playwright install-deps chromium` (line ~60). `playwright install-deps` runs `apt-get update` internally, so it SHOULD work — but the sandbox has no Docker so this hasn't been verified with an actual `docker build`. The original `backend/Dockerfile` uses the same pattern and works, so it's likely fine. Severity: **Low** (verification, not a known bug). The user should run `docker build -t task2sms .` locally before the first Railway deploy; if `playwright install-deps` fails, move it before the `rm -rf /var/lib/apt/lists/*` or run `apt-get update` again before it.
- [ ] **F-R4: Add CI workflow that builds the root Dockerfile on every PR** (added 2026-07-13 by Super Z, Session 2) — Since the sandbox can't build Docker images, the root `Dockerfile` is currently static-review-only. A GitHub Actions workflow (`.github/workflows/docker-build.yml`) that runs `docker build .` on every PR (and optionally `docker run` + hit `/api/health`) would catch layer-cache / apt-package / `npm ci` / Playwright issues before they hit Railway. Severity: **Medium**. Pair with a backend pytest workflow and a frontend build workflow for full CI coverage.
